# 1Day 项目部署到外网指南

本指南详细介绍如何将 1Day 创建的项目部署到 Cloudflare Pages，实现外网访问。

---

## 前置准备

- 一个 GitHub 账号（[注册地址](https://github.com/signup)）
- 一个 Cloudflare 账号（[注册地址](https://dash.cloudflare.com/sign-up)，可用 Google 账号登录）
- 1Day 导出的项目代码

---

## 第一部分：推送代码到 GitHub

### 1.1 创建 GitHub 仓库

1. 登录 GitHub，访问 [https://github.com/new](https://github.com/new)
2. 填写仓库信息：
   - **Repository name**：输入项目名称（如 `freego-wheelchair-app`）
   - **Description**：可选，填写项目描述
   - 选择 **Public**（公开）或 **Private**（私有）
3. **不要勾选**任何初始化选项（README、.gitignore 等）
4. 点击 **Create repository**

### 1.2 生成 Personal Access Token

GitHub 已不支持密码登录，需要创建 Token：

1. 访问 [https://github.com/settings/tokens](https://github.com/settings/tokens)
2. 点击 **Generate new token** → **Generate new token (classic)**
3. 填写配置：
   - **Note**：随便填写，如 `cloudflare-deploy`
   - **Expiration**：选择有效期（建议 90 days）
   - **Select scopes**：勾选 `repo`（整个选项）
4. 点击 **Generate token**
5. **立即复制**生成的 Token（格式如 `ghp_xxxxxxxxxxxx`）

> ⚠️ **重要**：Token 只显示一次，请妥善保存！

### 1.3 推送代码

打开终端，进入项目目录，执行以下命令：

```bash
# 1. 进入项目目录（替换为你的实际路径）
cd /你的项目路径

# 2. 添加 GitHub 远程仓库（替换为你的用户名和仓库名）
git remote add github https://github.com/你的用户名/你的仓库名.git

# 3. 确保代码在 master 分支
git checkout master

# 4. 如果代码在其他分支（如 onedaybot-dev），需要先合并
git merge onedaybot-dev -m "Merge for deployment"

# 5. 推送到 GitHub
git push -u github master
```

推送时会提示输入：
- **Username**：你的 GitHub 用户名
- **Password**：粘贴刚才复制的 Token（不是密码）

> 💡 **提示**：粘贴 Token 时不会显示任何字符，这是正常的，直接粘贴后按回车。

---

## 第二部分：部署到 Cloudflare Pages

### 2.1 登录 Cloudflare

1. 访问 [https://dash.cloudflare.com](https://dash.cloudflare.com)
2. 使用邮箱注册或 Google 账号登录

### 2.2 创建 Pages 项目

1. 在左侧菜单找到 **Build** → **Compute & AI** → **Workers & Pages**
2. 点击右上角 **+ Add** → 选择 **Pages**
3. 选择 **Import an existing Git repository** → 点击 **Get started**

### 2.3 连接 GitHub

1. 点击 **Connect to Git**
2. 选择 **GitHub**，授权 Cloudflare 访问你的 GitHub
3. 在 **Repository access** 选择：
   - **All repositories**（所有仓库）
   - 或 **Only select repositories**（仅选择特定仓库）
4. 点击 **Save** 保存授权
5. 选择你要部署的仓库（如 `freego-wheelchair-app`）

### 2.4 配置构建设置

在 **Set up builds and deployments** 页面填写：

| 配置项 | 值 |
|--------|-----|
| **Project name** | 项目名称（会生成 `项目名.pages.dev` 域名） |
| **Production branch** | `master` |
| **Framework preset** | `None`（保持默认） |
| **Build command** | `npm run build` |
| **Build output directory** | `dist` |

点击 **Save and Deploy** 开始部署。

### 2.5 等待部署完成

部署过程包含以下步骤：
1. ✅ Initializing build environment
2. ✅ Cloning git repository
3. ✅ Building application
4. ✅ Deploying to Cloudflare's global network

看到 **Success: Your site was deployed!** 表示部署成功。

---

## 第三部分：访问你的网站

部署成功后，你的网站地址为：

```
https://你的项目名.pages.dev
```

例如：`https://freego-wheelchair-app.pages.dev`

---

## 第四部分：后续更新

以后每次修改代码后，只需执行以下命令，Cloudflare 会自动重新部署：

```bash
# 在项目目录下执行
git add .
git commit -m "更新说明"
git push github master
```

---

## 常见问题

### Q1: 推送时提示 "remote github already exists"
说明已经添加过远程仓库，可以直接推送，或更新地址：
```bash
git remote set-url github https://github.com/用户名/仓库名.git
```

### Q2: 推送时提示 "Repository not found"
检查：
1. GitHub 上是否已创建该仓库
2. 远程地址是否正确（注意 `.git` 结尾）
3. Token 是否有 `repo` 权限

### Q3: 构建失败提示 "Could not read package.json"
代码没有完整推送，检查：
1. 确认当前分支是否包含完整代码
2. 如果代码在其他分支，需要先合并到 master

### Q4: 如何绑定自定义域名？
1. 在 Cloudflare Pages 项目页面点击 **Custom domains**
2. 点击 **Set up a custom domain**
3. 输入你的域名并按提示配置 DNS

---

## 总结

| 步骤 | 操作 |
|------|------|
| 1 | 创建 GitHub 仓库 |
| 2 | 生成 Personal Access Token |
| 3 | 推送代码到 GitHub |
| 4 | 登录 Cloudflare |
| 5 | 创建 Pages 项目并连接 GitHub |
| 6 | 配置构建命令和输出目录 |
| 7 | 部署完成，获得 `.pages.dev` 域名 |

---

*文档生成时间：2026-02-03*
